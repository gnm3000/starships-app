{
  "version": "2.0",
  "app_name": "starships-app",
  "stages": {
    "dev": {
      "api_gateway_stage": "api",
      "lambda_memory_size": 512,
      "lambda_timeout": 60,
      "automatic_layer": true,
      "layers": []
    }
  }
}
